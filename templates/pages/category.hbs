<div class="page">
    <div class="container">
        <h1 class="page-heading">{{category.name}}</h1>
        <div>{{{category.description}}}</div>
        <ul class="product-grid">
            {{#each products}}
            {{> product-card}}
            {{/each}}
        </ul>
    </div>
</div>